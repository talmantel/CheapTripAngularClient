const ROUTES_FOLDER = 'assets/new_json/partly';
const ROUTE_FOLDER = {
  mixed: 'routes',
  fixed: 'fixed_routes',
  flying: 'flying_routes',
};

const config = { ROUTES_FOLDER, ROUTE_FOLDER };

export default config;
